// Leave VITE_API_URL empty for local development (the Vite dev proxy handles /api).
// Set it to the deployed backend URL in production, e.g. https://leepr-backend.onrender.com
export const API_BASE = import.meta.env.VITE_API_URL ?? ''
